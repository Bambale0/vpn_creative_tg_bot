# 🛠️ Система плагинов VPN бота

## Обзор

Система плагинов позволяет расширять функциональность бота без изменения основного кода. Плагины могут добавлять новые команды, обработчики, меню и функциональность.

## 📁 Структура плагинов

```
vpnbot/
├── plugins/                 # Директория плагинов
│   ├── __init__.py          # Инициализация пакета плагинов
│   ├── example_plugin.py    # Пример плагина
│   └── your_plugin.py       # Ваш собственный плагин
├── utils/
│   └── plugins.py           # Менеджер плагинов
└── handlers/
    └── callback_handlers.py # Обработчики меню плагинов
```

## 🎯 Доступность

Меню плагинов доступно только администраторам бота. ID администраторов настраиваются в переменной окружения `ADMIN_IDS`.

## 📋 Требования к плагинам

Каждый плагин должен:

1. Находиться в файле с расширением `.py` в директории `plugins/`
2. Содержать класс `Plugin`
3. Реализовывать метод `setup()`
4. (Опционально) Реализовывать метод `teardown()` для очистки

### Пример плагина

```python
"""
Пример плагина для VPN бота
"""

class Plugin:
    """Базовый класс плагина"""
    
    version = "1.0.0"
    description = "Описание вашего плагина"
    author = "Ваше имя"
    
    def __init__(self):
        self.enabled = False
    
    def setup(self):
        """Инициализация плагина"""
        self.enabled = True
        
        # Здесь можно:
        # - Регистрировать обработчики команд
        # - Добавлять новые меню
        # - Интегрироваться с существующей функциональностью
        
        return True
    
    def teardown(self):
        """Очистка ресурсов плагина"""
        self.enabled = False
```

## 🚀 Использование

### Доступ к меню плагинов

1. Откройте бота в Telegram
2. Если вы администратор, в главном меню появится кнопка "🛠️ Плагины"
3. Нажмите на кнопку для доступа к менеджеру плагинов

### Функции менеджера плагинов

- **📊 Статистика плагинов** - обзор загруженных и доступных плагинов
- **🔧 Управление плагинами** - включение/выключение модулей
- **📦 Установить новый** - добавление новых плагинов
- **🗑️ Удалить плагин** - удаление ненужных модулей
- **🔄 Проверить обновления** - обновление существующих плагинов

## 🔧 API менеджера плагинов

### Основные функции

```python
from utils.plugins import plugin_manager

# Обнаружить все плагины
plugins = plugin_manager.discover_plugins()

# Загрузить все плагины
loaded_count = plugin_manager.load_all_plugins()

# Загрузить конкретный плагин
success = plugin_manager.load_plugin("example_plugin")

# Выгрузить плагин
success = plugin_manager.unload_plugin("example_plugin")

# Получить информацию о плагине
info = plugin_manager.get_plugin_info("example_plugin")

# Получить статистику
stats = plugin_manager.get_stats()
```

### Глобальные функции

```python
from utils.plugins import (
    setup_plugins,
    get_plugins_stats,
    get_plugin_info,
    load_plugin,
    unload_plugin
)

# Инициализация всех плагинов
setup_plugins()

# Получение статистики
stats = get_plugins_stats()

# Работа с конкретными плагинами
info = get_plugin_info("example_plugin")
load_plugin("example_plugin")
unload_plugin("example_plugin")
```

## 🎨 Создание собственного плагина

### 1. Создайте файл плагина

Создайте файл `plugins/my_plugin.py`:

```python
"""
Мой первый плагин для VPN бота
"""

from aiogram import types
from config.dependencies import t

class Plugin:
    """Плагин для демонстрации возможностей"""
    
    version = "1.0.0"
    description = "Мой первый плагин с дополнительной функциональностью"
    author = "Ваше имя"
    
    def __init__(self):
        self.enabled = False
        self.command_registered = False
    
    def setup(self):
        """Инициализация плагина"""
        self.enabled = True
        
        # Здесь можно зарегистрировать обработчики
        # from aiogram import Dispatcher
        # dp = Dispatcher.get_current()
        # dp.message.register(self.handle_my_command, commands=["mycommand"])
        
        return True
    
    def teardown(self):
        """Очистка ресурсов"""
        self.enabled = False
    
    async def handle_my_command(self, message: types.Message):
        """Обработчик пользовательской команды"""
        await message.answer("🎉 Это работает из моего плагина!")
```

### 2. Протестируйте плагин

```bash
python test_plugins.py
```

### 3. Перезапустите бота

Плагины автоматически загружаются при запуске бота.

## 🔒 Безопасность

- Плагины загружаются только из доверенной директории `plugins/`
- Доступ к меню плагинов имеют только администраторы
- Каждый плагин изолирован и не может повредить основную систему
- Перед загрузкой проверяется наличие обязательных методов

## 🐛 Отладка

### Логирование

Логи плагинов пишутся в общий лог бота с тегом `plugins`:

```bash
tail -f logs/bot.log | grep plugins
```

### Тестирование

```bash
# Тест системы плагинов
python test_plugins.py

# Тест интеграции меню
python test_menu_integration.py
```

### Проверка синтаксиса

```bash
python -m py_compile plugins/*.py
```

## 📝 Best Practices

1. **Именование** - используйте осмысленные имена для плагинов
2. **Версионирование** - указывайте версии ваших плагинов
3. **Обработка ошибок** - добавляйте try/except для критичных операций
4. **Документация** - документируйте функциональность вашего плагина
5. **Тестирование** - тестируйте плагины перед использованием в production

## 🚀 Примеры использования

### Добавление новой команды

```python
def setup(self):
    from aiogram import Dispatcher
    dp = Dispatcher.get_current()
    dp.message.register(self.handle_stats, commands=["mystats"])
    return True

async def handle_stats(self, message: types.Message):
    await message.answer("📊 Статистика из плагина!")
```

### Интеграция с существующей функциональностью

```python
def setup(self):
    # Можно использовать существующие утилиты
    from utils.game import check_subscription
    from config.config import DB_PATH
    
    # Добавить свою логику
    return True
```

### Создание сложных меню

```python
def setup(self):
    # Использование системы меню
    from utils.menu import create_custom_menu
    
    # Создать свое меню
    return True
```

## 📞 Поддержка

Если у вас возникли вопросы по созданию плагинов:

1. Изучите пример `plugins/example_plugin.py`
2. Проверьте логи для диагностики ошибок
3. Обратитесь к документации aiogram
4. Проверьте синтаксис вашего кода

## 🔄 Обновления

Система плагинов будет расширяться:

- [ ] Автоматическое обновление плагинов
- [ ] Графический интерфейс управления
- [ ] Магазин плагинов
- [ ] Система зависимостей
- [ ] Тестирование совместимости

---

**Примечание**: Система плагинов находится в активной разработке. API может меняться в будущих версиях.