# 🚀 Настройка Crypto Pay

## 📋 Шаг 1: Получение токена

1. Перейдите в [@CryptoBot](https://t.me/CryptoBot) в Telegram
2. Напишите `/start` и создайте бота
3. Перейдите в настройки → API токены
4. Создайте новый токен и скопируйте его

## 🔧 Шаг 2: Настройка окружения

Обновите файл `config_files/.env`:

```bash
# Crypto Pay
CRYPTO_PAY_TOKEN=ваш_реальный_токен_от_crypto_bot
CRYPTO_PAY_WEBHOOK_SECRET=ваш_секретный_ключ_для_вебхука
```

**Где взять секретный ключ:**
- Сгенерируйте случайную строку длиной 32+ символа
- Можно использовать команду: `openssl rand -hex 32`

## 🌐 Шаг 3: Настройка вебхука

Запустите скрипт настройки:

```bash
cd /root/vpnbot
python3 utils/setup_crypto_webhook.py
```

Или настройте вручную через Crypto Bot API:

```bash
curl -X POST https://pay.crypt.bot/api/setWebhook \
  -H "Crypto-Pay-API-Token: ваш_токен" \
  -d '{
    "url": "https://ваш-домен/crypto_webhook",
    "secret": "ваш_секретный_ключ"
  }'
```

## 🧪 Шаг 4: Тестирование

1. Запустите бота: `python3 main_scripts/main.py`
2. Проверьте меню оплаты → Crypto Pay
3. Протестируйте создание инвойса

## 🔍 Проверка статуса

Проверить настройки вебхука:

```bash
curl -X GET https://pay.crypt.bot/api/getWebhookInfo \
  -H "Crypto-Pay-API-Token: ваш_токен"
```

## 🛠️ Устранение проблем

### ❌ Ошибка "Invalid token"
- Проверьте правильность токена
- Убедитесь, что токен активен в @CryptoBot

### ❌ Ошибка вебхука
- Проверьте, что ваш сервер доступен из интернета
- Убедитесь, что порт 443 открыт для входящих соединений
- Проверьте SSL сертификат (должен быть валидный HTTPS)

### ❌ Ошибка подписи вебхука
- Убедитесь, что `CRYPTO_PAY_WEBHOOK_SECRET` одинаковый в .env и в настройках Crypto Pay

## 💡 Особенности Crypto Pay

- Поддерживаемые валюты: USDT, BTC, ETH, TON
- Комиссия: 0% на создание инвойса, комиссия сети на перевод
- Минимальная сумма: зависит от валюты
- Время жизни инвойса: 15 минут

## 📊 Мониторинг

Статистика платежей доступна в админке:
- Общая сумма крипто-платежей
- Количество успешных транзакций
- Популярные валюты

## 🔒 Безопасность

- Все вебхуки проверяются по подписи
- Секретный ключ хранится в .env файле
- Автоматическая проверка статуса платежей

## 🚀 Готово!

После настройки пользователи смогут оплачивать подписку криптовалютой через удобный интерфейс в Telegram.