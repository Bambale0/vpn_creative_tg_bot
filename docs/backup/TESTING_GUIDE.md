# 🧪 РУКОВОДСТВО ПО ТЕСТИРОВАНИЮ VPN TELEGRAM BOT

## 📋 Обзор тестовой системы

Система тестирования включает:
- **Unit тесты** - тестирование отдельных модулей
- **Интеграционные тесты** - тестирование взаимодействия компонентов
- **Тесты безопасности** - проверка уязвимостей и конфигураций
- **Тесты производительности** - проверка скорости и эффективности

## 🚀 Быстрый старт

### Установка зависимостей
```bash
# Основные зависимости тестирования
pip install -r requirements-test.txt

# Или установка отдельных пакетов
pip install pytest pytest-asyncio psutil
```

### Запуск всех тестов
```bash
# Через скрипт
python run_tests.py

# Через pytest напрямую
python -m pytest tests/ -v

# С покрытием кода
python -m pytest tests/ --cov=main,webapp,config,handlers,utils --cov-report=term-missing
```

### Запуск отдельных категорий тестов
```bash
# Только unit тесты
python -m pytest tests/unit/ -v

# Только интеграционные тесты
python -m pytest tests/integration/ -v

# Только тесты безопасности
python -m pytest tests/security/ -v

# Конкретный тестовый файл
python -m pytest tests/unit/test_config.py -v
```

## 📁 Структура тестов

```
tests/
├── unit/                 # Unit тесты
│   ├── test_config.py    # Тесты конфигурации
│   └── test_utils.py      # Тесты утилит
├── integration/          # Интеграционные тесты
│   ├── test_bot_integration.py  # Тесты интеграции бота
│   ├── test_monitoring.py        # Тесты мониторинга
│   └── test_performance.py      # Тесты производительности
├── security/             # Тесты безопасности
│   └── test_security.py  # Основные тесты безопасности
└── conftest.py           # Конфигурация pytest
```

## 🔧 Настройка тестовой среды

### Переменные окружения для тестов
```bash
export TEST_MODE=true
export DB_PATH=:memory:
export TELEGRAM_TOKEN=test_token_123
export ADMIN_USER=test_admin
export ADMIN_PASS=test_password
```

### Конфигурация pytest
Файл `pytest.ini` содержит настройки:
- Автоматическое определение асинхронных тестов
- Игнорирование служебных директорий
- Настройки покрытия кода

## 🧪 Типы тестов

### Unit тесты
- Тестирование отдельных функций и классов
- Изоляция от внешних зависимостей
- Быстрое выполнение

### Интеграционные тесты
- Тестирование взаимодействия компонентов
- Проверка работы с базой данных
- Тестирование API endpoints

### Тесты безопасности
- Проверка конфигурационных файлов
- Анализ зависимостей на уязвимости
- Проверка прав доступа к файлам

### Тесты производительности
- Измерение времени выполнения операций
- Контроль использования памяти
- Бенчмаркинг критических компонентов

## 🚨 Обнаруженные проблемы безопасности

Тесты безопасности выявили следующие проблемы:

### 1. Чувствительные данные в .env файле
**Проблема**: В файле `.env` обнаружены чувствительные данные в открытом виде
**Решение**: 
- Использовать шифрование для чувствительных данных
- Реализовать ротацию секретов
- Использовать vault или secure storage

### 2. Излишние права доступа к файлам
**Проблема**: Файлы конфигурации имеют слишком широкие права доступа
**Решение**:
```bash
# Установить правильные права
chmod 600 config/config.py
chmod 700 ssl/
```

## 📊 Покрытие кода

Для измерения покрытия кода:
```bash
# Установить coverage
pip install coverage pytest-cov

# Запуск с измерением покрытия
python -m pytest tests/ --cov=main,webapp,config,handlers,utils --cov-report=html

# Просмотр отчета
open htmlcov/index.html
```

## 🔄 Непрерывная интеграция

### Пример .github/workflows/test.yml
```yaml
name: Tests
on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
    - name: Install dependencies
      run: |
        pip install -r requirements.txt
        pip install -r requirements-test.txt
    - name: Run tests
      run: python -m pytest tests/ -v
    - name: Security scan
      run: python -m pytest tests/security/ -v
```

## 🐛 Отладка тестов

### Включение подробного вывода
```bash
# Очень подробный вывод
python -m pytest tests/ -v -s

# Вывод с трассировкой
python -m pytest tests/ --tb=long

# Запуск только failed тестов
python -m pytest tests/ --last-failed
```

### Отладка с pdb
```python
import pdb; pdb.set_trace()  # Добавить в тест для отладки
```

## 📈 Метрики качества

- **Покрытие кода**: > 70%
- **Время выполнения тестов**: < 30 секунд
- **Количество unit тестов**: > 50
- **Количество интеграционных тестов**: > 20
- **Количество security тестов**: > 10

## 🎯 Best Practices

1. **Пишите тесты до кода** (TDD)
2. **Тестируйте граничные условия**
3. **Используйте моки для внешних зависимостей**
4. **Замеряйте производительность критических путей**
5. **Регулярно запускайте security сканирование**
6. **Интегрируйте тесты в CI/CD pipeline**

## 🔗 Полезные ссылки

- [Документация pytest](https://docs.pytest.org/)
- [pytest-asyncio](https://pytest-asyncio.readthedocs.io/)
- [Python testing](https://docs.python.org/3/library/unittest.html)
- [Security testing best practices](https://owasp.org/www-project-top-ten/)