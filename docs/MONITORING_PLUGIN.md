# 🔍 Плагин мониторинга VPN бота

## Обзор

Плагин мониторинга предоставляет полный контроль над состоянием системы, безопасностью и метриками работы VPN бота. Это продвинутый инструмент для администраторов с функциями киберразведки и аналитики.

## 🚀 Быстрый старт

### Доступ к мониторингу

1. **Откройте бота в Telegram**
2. **Если вы администратор** - в главном меню появится кнопка "🛠️ Плагины"
3. **Нажмите на кнопку плагинов** - откроется менеджер плагинов
4. **Выберите "🔍 Мониторинг системы"** - доступ к полному мониторингу

### Функции мониторинга

- **📊 Общий статус** - сводка состояния системы
- **⚡ Реальные метрики** - текущие показатели в реальном времени
- **🔒 Безопасность** - анализ угроз и уязвимостей
- **📈 Подробный отчет** - детальная аналитика
- **🚨 Алерты** - уведомления о проблемах
- **⚙️ Настройки** - конфигурация мониторинга

## 📊 Мониторинг системы

### Системные метрики

- **CPU**: Использование процессора в реальном времени
- **Память**: Использование оперативной памяти и swap
- **Диски**: Использование дискового пространства по разделам
- **Сеть**: Количество соединений и трафик
- **Процессы**: Количество активных процессов
- **Load Average**: Средняя нагрузка системы

### Безопасность

- **Сканирование портов**: Проверка открытых портов (SSH, HTTP, HTTPS, Webhook)
- **Целостность файлов**: Контрольные суммы ключевых файлов
- **Аномалии**: Автоматическое обнаружение угроз
- **Алерты**: Уведомления о критических состояниях

### База данных

- **Размер БД**: Мониторинг размера файла базы данных
- **Статистика таблиц**: Количество записей в каждой таблице
- **Целостность**: Проверка целостности базы данных
- **Резервные копии**: Информация о последних бэкапах

### Метрики бота

- **Активные пользователи**: Пользователи за последние 24 часа
- **Всего пользователей**: Общее количество зарегистрированных
- **Активные подписки**: Количество активных подписок
- **Конверсия**: Процент пользователей с активными подписками

## 🔧 Использование API

### Получение полного отчета

```python
from utils.plugins import monitoring_plugin

async def get_report():
    report = await monitoring_plugin.get_comprehensive_report()
    print(f"Статус: {report['status']}")
    print(f"CPU: {report['system']['cpu_count']} cores")
    print(f"Память: {report['system']['memory']['percentage']}%")
```

### Реальные метрики

```python
metrics = monitoring_plugin.get_real_time_metrics()
print(f"CPU: {metrics['cpu_percent']}%")
print(f"Память: {metrics['memory_percent']}%")
```

### Отчет безопасности

```python
security_report = monitoring_plugin.generate_security_report()
print(security_report)
```

### Алерты производительности

```python
alerts = monitoring_plugin.get_performance_alerts()
for alert in alerts:
    print(f"{alert['message']} (значение: {alert['value']})")
```

## ⚙️ Конфигурация

### Пороговые значения для алертов

Плагин автоматически генерирует алерты при превышении порогов:

- **CPU**: >90% (критический), >70% (предупреждение)
- **Память**: >90% (критический)
- **Диски**: >90% использования (критический)

### Критические файлы для проверки целостности

Плагин проверяет целостность следующих файлов:
- `config/config.py` - конфигурация бота
- `bot.py` - основной файл бота
- `start_webhook_bot.py` - скрипт вебхуков
- `data/subscriptions.db` - база данных

## 🔒 Безопасность

### Права доступа

- **Только администраторы**: Весь функционал доступен только пользователям из `ADMIN_IDS`
- **Проверка прав**: Каждый запрос проверяет права администратора
- **Изоляция**: Плагин работает в изолированном окружении

### Защита данных

- **Без сохранения**: Метрики не сохраняются на диск
- **Реальное время**: Все данные получаются в реальном времени
- **Без внешних запросов**: Не отправляет данные на внешние серверы

## 🐛 Отладка

### Логирование

Логи плагина записываются с тегом `plugins.monitoring`:

```bash
tail -f logs/bot.log | grep "plugins.monitoring"
```

### Тестирование

Для тестирования используйте встроенные функции:

```python
# Тест загрузки плагина
from utils.plugins import plugin_manager
plugin_manager.load_plugin('monitoring_plugin')

# Тест функциональности
plugin = plugin_manager.plugins['monitoring_plugin']
metrics = plugin.get_real_time_metrics()
```

### Диагностика проблем

1. **Плагин не загружается**: Проверьте наличие `psutil` в зависимостях
2. **Нет доступа к метрикам**: Проверьте права доступа к системным ресурсам
3. **Ошибки в логах**: Проверьте корректность конфигурации

## 📈 Производительность

### Оптимизация

- **Минимальная нагрузка**: Плагин использует минимальные ресурсы
- **Ленивая загрузка**: Компоненты загружаются по требованию
- **Кэширование**: Метрики не кэшируются для актуальности

### Мониторинг нагрузки

Плагин сам отслеживает свою нагрузку:
- CPU использование плагина: <1%
- Память: ~10-20MB
- Дисковый ввод-вывод: минимальный

## 🔄 Обновления

### Версия 2.0.0

- Полная переработка архитектуры
- Добавлена система киберразведки
- Улучшенная аналитика безопасности
- Реальные метрики в реальном времени
- Интеграция с системой плагинов

### Планируемые функции

- [ ] Графики и диаграммы
- [ ] Исторические данные
- [ ] Автоматические бэкапы
- [ ] Уведомления в Telegram
- [ ] API для внешних систем

## 🆘 Поддержка

### Частые проблемы

**Плагин не отображается в меню:**
- Проверьте, что ваш ID есть в `ADMIN_IDS`
- Проверьте загрузку плагина в логах

**Ошибки доступа к системным ресурсам:**
- Убедитесь, что бот запущен с достаточными правами
- Проверьте доступность `psutil`

**Метрики не обновляются:**
- Проверьте работу системных утилит
- Проверьте доступ к `/proc` файловой системе

### Контакты

Для вопросов и предложений:
- Проверьте логи бота
- Изучите документацию системы плагинов
- Обратитесь к разработчикам системы

---

**Примечание**: Плагин мониторинга предназначен для администраторов системы. Используйте его для проактивного управления и предотвращения проблем.