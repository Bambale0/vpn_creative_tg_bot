# 🧪 Тестирование VPN бота

Этот документ описывает систему тестирования проекта VPN бота.

## 📋 Обзор

Проект включает в себя:
- **Unit тесты** - тестирование отдельных функций и классов
- **Integration тесты** - тестирование взаимодействия между компонентами
- **Проверку покрытия кода** - анализ покрытия тестами
- **Проверку синтаксиса** - валидация Python кода

## 🚀 Быстрый старт

### Запуск всех тестов

```bash
# Запуск через pytest
python -m pytest tests/ -v

# Запуск через наш скрипт
python run_tests.py

# Запуск с покрытием кода
python -m pytest tests/ --cov=. --cov-report=html
```

### Запуск отдельных типов тестов

```bash
# Только unit тесты
python -m pytest tests/unit/ -v

# Только integration тесты
python -m pytest tests/integration/ -v

# Тесты с покрытием
python -m pytest tests/ --cov=. --cov-report=term-missing
```

## 📁 Структура тестов

```
tests/
├── conftest.py              # Общие фикстуры и конфигурация
├── README.md               # Этот файл
├── __pycache__/           # Кэш Python
├── unit/                  # Unit тесты
│   ├── __init__.py
│   ├── test_plugins.py    # Тесты плагинов
│   ├── test_game.py       # Тесты игровых функций
│   ├── test_callback_handlers.py  # Тесты обработчиков
│   └── test_*.py          # Другие unit тесты
└── integration/           # Integration тесты
    ├── __init__.py
    ├── test_plugins_integration.py  # Интеграционные тесты плагинов
    └── test_*.py          # Другие integration тесты
```

## 🧪 Типы тестов

### Unit тесты

Тестируют отдельные функции и классы в изоляции:

- **test_plugins.py** - тестирование плагинов мониторинга и бонусов
- **test_game.py** - тестирование игровых механик (очки, достижения, trial)
- **test_callback_handlers.py** - тестирование обработчиков callback'ов

### Integration тесты

Тестируют взаимодействие между компонентами:

- **test_plugins_integration.py** - интеграция плагинов с системой

## 🔧 Конфигурация

### pytest.ini

Основная конфигурация pytest:

```ini
[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    --verbose
    --tb=short
    --strict-markers
    --disable-warnings
    --cov=.
    --cov-report=html:htmlcov
    --cov-report=term-missing
    --cov-fail-under=80
markers =
    unit: Unit tests
    integration: Integration tests
    slow: Slow running tests
```

### conftest.py

Общие фикстуры и настройки для всех тестов:

- Настройка путей импорта
- Тестовые переменные окружения
- Event loop для асинхронных тестов

## 📊 Покрытие кода

### Требования к покрытию

- Минимальное покрытие: **80%**
- Целевые модули: `utils/`, `handlers/`, `plugins/`

### Отчет покрытия

После запуска тестов с покрытием создается HTML отчет:

```bash
python -m pytest tests/ --cov=. --cov-report=html
```

Отчет доступен в `htmlcov/index.html`

## 🛠️ Написание тестов

### Пример unit теста

```python
import pytest
from unittest.mock import patch, AsyncMock

class TestExample:

    @pytest.mark.asyncio
    async def test_example_function(self):
        """Тестирование примера функции"""
        with patch('module.function') as mock_func:
            mock_func.return_value = "test_result"

            result = await example_function()

            assert result == "test_result"
            mock_func.assert_called_once()
```

### Пример integration теста

```python
import pytest

class TestIntegration:

    @pytest.mark.asyncio
    async def test_component_interaction(self):
        """Тестирование взаимодействия компонентов"""
        # Тест взаимодействия между модулями
        pass
```

## 🎯 Маркеры тестов

- `@pytest.mark.unit` - unit тесты
- `@pytest.mark.integration` - integration тесты
- `@pytest.mark.slow` - медленные тесты
- `@pytest.mark.asyncio` - асинхронные тесты

## 🔍 Отладка тестов

### Запуск с отладкой

```bash
# Подробный вывод
python -m pytest tests/ -v -s

# Остановка при первой ошибке
python -m pytest tests/ -x

# Показать локальные переменные
python -m pytest tests/ --pdb
```

### Анализ покрытия

```bash
# Детальный отчет покрытия
python -m pytest tests/ --cov=. --cov-report=term-missing

# HTML отчет
python -m pytest tests/ --cov=. --cov-report=html

# Исключить тестовые файлы из покрытия
python -m pytest tests/ --cov=. --cov-report=html --cov-skip=test*
```

## 🚨 CI/CD интеграция

### GitHub Actions

Пример workflow для автоматического тестирования:

```yaml
name: Tests
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.9'
    - name: Install dependencies
      run: |
        pip install -r requirements.txt
        pip install pytest pytest-cov
    - name: Run tests
      run: python -m pytest tests/ --cov=. --cov-report=xml
    - name: Upload coverage
      uses: codecov/codecov-action@v2
```

## 📈 Метрики качества

### Целевые показатели

- **Покрытие кода**: ≥ 80%
- **Успешность тестов**: ≥ 95%
- **Время выполнения**: ≤ 30 секунд
- **Количество тестов**: ≥ 50

### Мониторинг

- Отчеты покрытия в `htmlcov/`
- Результаты тестов в логах CI/CD
- Метрики в issue tracker

## 🐛 Поиск и исправление ошибок

### Анализ логов

```bash
# Проверка логов на ошибки
tail -f logs/service-error.log

# Поиск конкретных ошибок
grep -r "ERROR" logs/
```

### Локальная отладка

```bash
# Запуск конкретного теста
python -m pytest tests/unit/test_plugins.py::TestMonitoringPlugin::test_setup_success -v -s

# Запуск с отладчиком
python -m pytest tests/unit/test_game.py::TestGameFunctions::test_add_user_points_success --pdb
```

## 📚 Ресурсы

- [pytest документация](https://docs.pytest.org/)
- [pytest-cov документация](https://pytest-cov.readthedocs.io/)
- [Python testing best practices](https://docs.python.org/3/library/unittest.html)

## 🤝 Вклад в тестирование

### Добавление новых тестов

1. Создайте файл в соответствующей директории (`tests/unit/` или `tests/integration/`)
2. Назовите файл `test_*.py`
3. Используйте существующие фикстуры из `conftest.py`
4. Следуйте naming convention: `test_*` для функций, `Test*` для классов

### Правила написания тестов

- **Один тест - одна проверка**
- **Ясные имена** для тестов и фикстур
- **Моки для внешних зависимостей**
- **Асинхронные тесты** для async функций
- **Документирование** сложных тестов

## 📞 Поддержка

При проблемах с тестированием:

1. Проверьте логи в `logs/`
2. Запустите тесты с флагом `-v` для подробного вывода
3. Проверьте покрытие кода в `htmlcov/`
4. Создайте issue с описанием проблемы
